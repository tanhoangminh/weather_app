name: core_data
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  core_dependency:
    path: ../core_dependency
  core_router:
    path: ../core_router

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: null
  injectable_generator: null
  envied_generator:
  retrofit_generator:
  hive_generator: